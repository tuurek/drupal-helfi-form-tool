*** Settings ***
Documentation       A resource file with reusable keywords and variables.
Library             String

*** Variables ***
${BASEURL}     		%{BASEURL}	#Base url to test, set to environment variables when testing
